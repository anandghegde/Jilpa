// JilpaIPC: the socket protocol shared by the app and the `jilpa` CLI.
//
// May depend on: Foundation.
//
// Newline-delimited, versioned JSON over a Unix socket in Application Support, mode 0600, peer uid
// checked with LOCAL_PEERCRED. Code lands with WP16.
